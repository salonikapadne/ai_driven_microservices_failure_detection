# Core
python-dotenv==1.0.1

# LangGraph & LangChain
langgraph>=0.2.0
langchain-core>=0.3.0

# AI / LLM backends (use whichever you have credentials for)
google-cloud-aiplatform>=1.38.0
google-generativeai>=0.3.0
google-auth>=2.25.0
openai>=1.12.0

# RabbitMQ consumer
pika>=1.3.0

# Dashboard Socket.IO server (bridges ai_engine output to React frontend)
flask>=3.0.0
flask-socketio>=5.3.0

# Testing
pytest==8.0.0
pytest-cov==4.1.0
